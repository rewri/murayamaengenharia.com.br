$(document).ready(function() {

    // CAROUSEL
    function initCarousel() {
        $('.carousel').carousel({
            interval: 6000,
            pause: false
        });
    }

    initCarousel();


});
